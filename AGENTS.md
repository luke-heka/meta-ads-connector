# Agent instructions

This repo keeps its agent configuration in [`CLAUDE.md`](CLAUDE.md). Read that file —
it is the source of truth for every agent, not just Claude.

It covers the issue tracker (Linear, team `DP`), the triage label vocabulary, and the
domain-doc layout, each pointing at a file under `docs/agents/`.
